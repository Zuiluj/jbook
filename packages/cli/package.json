{
  "name": "jsbook-proto",
  "version": "2.0.0",
  "description": "",
  "publishConfig": {
    "access": "public"
  },
  "bin": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "start": "tsc --watch --preserveWatchOutput",
    "prePublishOnly": "esbuild src/index.ts --platform=node --outfile=dist/index.js --bundle --minify --define:process.env.NODE_ENV=\\\"production\\\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@jbook-proto/local-client": "^1.0.0"
  },
  "devDependencies": {
    "@jsbook-proto/local-api": "^2.0.0",
    "@types/node": "^14.14.35",
    "commander": "^7.1.0",
    "esbuild": "0.8.26",
    "typescript": "^4.2.3"
  }
}
